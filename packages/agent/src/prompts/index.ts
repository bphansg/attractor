/**
 * @attractor/agent — Prompt Construction
 *
 * Barrel export of system prompt builder, environment context, and
 * project documentation discovery.
 */

export { buildSystemPrompt } from './builder.js';
export { buildEnvironmentContext } from './context.js';
export { discoverProjectDocs } from './project-docs.js';
